import { Fonts } from "./fonts";

export const Typography = {
  body: {
    fontFamily: Fonts.sans,
    fontSize: 16,
    lineHeight: 24,
  },
  title: {
    fontFamily: Fonts.sans,
    fontSize: 24,
    lineHeight: 32,
  },
  caption: {
    fontFamily: Fonts.sans,
    fontSize: 14,
    lineHeight: 20,
  },
  label: {
    fontFamily: Fonts.sans,
    fontSize: 12,
    lineHeight: 16,
  },
  button: {
    fontFamily: Fonts.sans,
    fontSize: 14,
    lineHeight: 20,
  }
}